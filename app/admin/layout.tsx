'use client'

const LayoutHeader = ({
    children,
  }: {
    children: React.ReactNode
  }) =>{

    return (
        <div>
            {children}
        </div>
    );

}

export default LayoutHeader;